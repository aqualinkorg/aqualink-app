version: '3.4'
services:
  aqua-postgres:
    image: kartoza/postgis:12.1
    restart: always
    volumes:
      - ./db_data:/var/lib/postgresql/data
    ports:
      - 54321:5432
    environment:
      POSTGRES_PASSWORD: '!ChangeMe!'

volumes:
  db_data:
